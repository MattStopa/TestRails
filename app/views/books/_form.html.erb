<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>



  <div class="field">
    <%= form.label :publication_year %>
    
    <div data-controller="hello">
      <%= book.publication_year %>

      <% ['Animal' , 'Vegetable', 'Mineral'].each do |item| %>
        <div data-action="click->hello#greet" data-info="<%= item %>" >
          <%= item %>
        </div>
      <% end %>
      <input data-target="hello.publicationYear" name="book[author]" type="hidden">
      # <button >Greet</button>
    </div>

    
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
